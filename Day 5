//Write a program to identify if the number is even or odd.
#include <iostream>
using namespace std;
int main () {
    int a;
    cout<<"Enter a number : ";
    cin>>a;
    if (a % 2 == 0)
    cout<<"Even";
    else
    cout<<"Odd";
    return 0;
}
