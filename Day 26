//write a program to calculate Maximum number of handshakes

#include <iostream.h>

int main() {
  int N, sum = 0;
  cout<<"Enter the num of people in the room : ";
  cin>>N;
  int temp = N;
  while(N > 0) { 
    N--;
    sum += N;
  }
  cout<<"Total handshakes = "<<sum;
  return 0;
}
