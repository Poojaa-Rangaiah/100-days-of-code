//Write a program to find the Factors of a number.
#include <iostream>
using namespace std;
int main() {
 int n, rem;
 cout<<"Enter a number : ";
 cin>>n;
 cout<<"The factors of a number are";
 for (int i = 1; i < n; i++) {
  rem = n % i;
  if (rem == 0) {
    cout<<", "<<i;
  }  
 }
 return 0;
}
